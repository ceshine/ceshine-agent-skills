interface:
  display_name: "Generate Commit Message"
  short_description: "Draft a commit message from staged changes"
  default_prompt: "Use $generate-commit-message to review staged changes and draft a commit message."
